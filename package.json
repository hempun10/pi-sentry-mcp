{
  "name": "pi-sentry-mcp",
  "version": "0.2.0",
  "description": "Sentry MCP for Pi — hosted https://mcp.sentry.dev/mcp with OAuth",
  "keywords": [
    "pi-package",
    "pi",
    "mcp",
    "sentry",
    "effect"
  ],
  "license": "MIT",
  "author": "hempun10",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/hempun10/pi-sentry-mcp.git"
  },
  "homepage": "https://github.com/hempun10/pi-sentry-mcp#readme",
  "bugs": {
    "url": "https://github.com/hempun10/pi-sentry-mcp/issues"
  },
  "engines": {
    "node": ">=20"
  },
  "files": [
    "extensions",
    "src",
    "skills",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "dependencies": {
    "effect": "^3.22.2"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "pi-mcp-adapter": "*"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.85.1",
    "@types/node": "^24.5.2",
    "typescript": "^5.9.3"
  },
  "pi": {
    "extensions": [
      "./extensions/sentry.ts"
    ],
    "skills": [
      "./skills"
    ]
  }
}
